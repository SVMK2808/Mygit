// Shared utilities used by CLI command implementations.
#include "mygit/cli/command.h"
#include "mygit/repository/repository.h"

namespace mygit {
    namespace cli {
        // intentionally empty — each command is in its own file
    } // namespace cli
} // namespace mygit
